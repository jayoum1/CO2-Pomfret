/**
 * This page has been merged into the unified "Forest Insights" page at /.
 * Redirect any existing bookmarks or links.
 */
import { redirect } from 'next/navigation'

export default function VisualizationsRedirect() {
  redirect('/')
}
